#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@ionic/core/node_modules/@stencil/core/bin/stencil" "$@"
  ret=$?
else 
  node  "$basedir/../@ionic/core/node_modules/@stencil/core/bin/stencil" "$@"
  ret=$?
fi
exit $ret
